<Window x:Class="StartItUp.View.Main.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StartItUp.View.Main"
        mc:Ignorable="d"
        Title="StartItUp"
        Height="250" Width="400"
        WindowStyle="ToolWindow" 
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Content="Automatically startup with the system." />
        <ListView Grid.Row="1" x:Name="ltvStartupProfiles">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <local:ProfileUC />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0 5">
            <Button x:Name="btnNew" Content="New" Width="50"/>
            <Button x:Name="btnEdit" Content="Edit" Width="50" Margin="10 0 10 0"/>
            <Button x:Name="btnDelete" Content="Delete" Width="50"/>
        </StackPanel>
    </Grid>
</Window>
